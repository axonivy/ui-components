import { style } from '@vanilla-extract/css';

export const customEdge = style({
  position: 'absolute',
  background: 'white',
  padding: '2px 5px',
  borderRadius: '4px',
  fontSize: '12px',
  whiteSpace: 'nowrap'
});
